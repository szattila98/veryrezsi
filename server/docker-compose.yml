version: '3.1'

services:

  database:
    image: mysql:8.0.29
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    ports: 
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: password
